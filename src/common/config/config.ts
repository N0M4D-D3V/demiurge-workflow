export const APPS: string[] = ["Powergit", "Opener"];

export const POWERGIT_MENU: string[] = [
  "git status",
  "git clone",
  "git fetch",
  "git pull",
  "git push",
];

// the fileopener menu will be generated dynamically
